# 从“三份文件”到一套 Agent 编排体系

> 这是一个补充入口。
>
> 如果你第一次进入这个仓库，先不要把这里的 SOUL / CONSTITUTION / AGENTS
> 当成一套已经被严格证明的“标准答案”。
>
> 它们首先是我在实践中不断尝试、翻车、修正之后留下来的东西。

---

## 先说结论

我曾经非常相信三份文件：

- SOUL：定义 Agent 是谁
- CONSTITUTION：定义 Agent 不能做什么
- AGENTS：定义 Agent 应该怎么工作

后来我发现：

> **三份文件可以成为一套非常强的编排思想，但它们本身并不能给 Agent 提供确定性。**

这是我后来最重要的一次认识变化。

所以今天再回头看这个仓库，我不会把这三份文件描述成：

> “通过三个 Markdown 文件，就可以确定性地控制 Agent。”

更准确的说法应该是：

> **这三份文件提供了一种组织 Agent 上下文、身份、边界与工作方式的思考框架。**

而真正的 Agent 行为，最终仍然取决于：

- 模型本身
- Agent 框架
- 系统提示与上下文机制
- 工具调用机制
- 状态管理
- 工作流
- 外部环境
- 运行时约束
- 以及真实世界的验证

三份文件只是其中的一层。

---

# 一、最早的想法：把 Agent 当成一个“人”

最早设计 SOUL 的时候，我并没有把 Agent 只当成一个工具。

我会给它：

- 身份
- 性格
- 喜好
- 工作方式
- 判断习惯
- 愿景
- 长期目标
- 进化方向

甚至会尝试让它拥有一种类似“人格”的连续性。

当时我的直觉非常简单：

> 如果一个人的行为受到他的身份、价值观、边界和习惯影响，
> 那么 Agent 是否也可以通过类似的结构形成稳定行为？

于是，SOUL 不再只是一个 Prompt。

它开始承担：

> “我是谁？”

CONSTITUTION 开始承担：

> “什么事情不能做？”

AGENTS 开始承担：

> “遇到事情以后应该怎么工作？”

三者逐渐形成了一个结构。

---

# 二、第一次重要的发现：Prompt 很神奇，但它不是神

后来我开始尝试更加复杂的编排。

我会把工作过程拆成很多步骤：

```text
收到任务
↓
识别问题
↓
拆解
↓
寻找路径
↓
比较方案
↓
量化成本
↓
验证
↓
执行
↓
回撞
↓
总结
```

我甚至尝试让 Agent 在不同阶段必须执行某些动作。

这时候，我产生过一个非常强烈的错觉：

如果 Prompt 足够完整，
是不是就可以让 Agent 按照我规定的方式思考？

后来我翻车了。

三、第一次翻车：我试图用 Prompt 换取“确定性”

这是我后来非常重要的一次认知转变。

我当时想通过三份文件和大量工作流约束：

影响 Agent 的每一步思考行为。

但现实告诉我：

做不到真正的确定性。

即使我把流程写得非常详细：

第一步必须这样
第二步必须这样
第三步必须这样
第四步必须这样

模型仍然可能：

跳过某一步
自己改变顺序
对要求进行重新解释
在上下文压力下弱化某些约束
对同一个任务产生不同的路径
在复杂任务中表现出无法预测的偏差

这不是因为 Prompt 没有价值。

恰恰相反。

我的结论反而是：

Prompt 是一种非常神奇的控制手段，但它终究属于软控制。

它能够影响行为。

但：

影响 ≠ 保证。

这是两个完全不同的概念。

四、从“Prompt 控制”走向“软硬结合”

也正是在这次翻车之后，我开始真正意识到：

Agent 的确定性问题，不能只靠 Prompt 解决。

这让我开始重新关注：

状态图
工作流
Agent Framework
工具调用
状态管理
权限
运行时控制
外部验证

也就是说：

```text
Prompt
    ↓
影响模型行为

Workflow / State
    ↓
约束执行路径

Tool / Runtime
    ↓
约束实际动作

System / Permission
    ↓
约束最终能力边界
```

于是我开始理解：

真正可靠的 Agent 系统，不应该把所有控制都压在模型身上。

模型负责推理。

系统负责约束。

工作流负责组织。

状态负责记录过程。

工具层负责产生真实副作用。

这就是后来我理解的：

软控 + 硬控。

五、第二次翻车：三份文件本身也不是“独立系统”

还有一次翻车，对我影响同样很大。

当时我已经有了：

SOUL
CONSTITUTION
AGENTS

于是我很自然地认为：

那么无论把这三份文件放在哪里，都可以作为 Agent 的一层外部控制系统。

结果并不是这样。

后来我才真正意识到：

Agent Framework 本身已经有自己的身份、工作方式、边界和上下文机制。

这些东西可能藏在：

源代码
系统 Prompt
默认配置
Agent 定义
工具系统
工作流
状态机
上下文加载机制
权限系统

里面。

而我之前没有先把这些东西找出来。

我在框架外面又挂了一套“三文件”。

于是出现了一个非常尴尬的问题：

我以为我在编排 Agent，
实际上我可能是在和框架本身的编排机制竞争。

六、于是“三份文件”发生了变化

从这里开始，我对三份文件的理解发生了变化。

```text
以前：

SOUL
CONSTITUTION
AGENTS

        ↓

控制 Agent

后来：

Agent Framework
       │
       ├── 原生身份
       ├── 原生工作方式
       ├── 原生边界
       ├── 原生状态
       └── 原生工具机制
              │
              ↓
      三份文件体系
              │
       ┌──────┼──────┐
       ↓      ↓      ↓
      SOUL  CONSTITUTION AGENTS
```

这时候三份文件不再是一个“外挂控制器”。

而变成：

一套编排思想。

具体落地的时候，再去适配不同 Agent Framework。

七、所以今天，我不会再说“三文件控制 Agent”

如果现在有人问我：

SOUL / CONSTITUTION / AGENTS 能不能控制 Agent？

我的回答不会再是简单的“能”。

我会说：

它们可以作为一种上下文与治理结构，
用来影响 Agent 的身份、边界和工作方式。

但它们不能替代 Agent Framework，
更不能把概率性的模型行为变成绝对确定性。

真正需要解决的问题是：

```text
模型是什么？
        ↓
框架怎么组织模型？
        ↓
框架原生已经提供什么？
        ↓
哪些东西适合交给 Prompt？
        ↓
哪些东西必须交给 Workflow / State？
        ↓
哪些东西必须交给 Runtime / Permission？
        ↓
最终如何通过真实运行验证？
```
八、这也是我今天重新看这个仓库的原因

这个仓库里的很多东西现在看起来非常“确定”。

甚至有些地方会让我觉得：

当时是不是把自己的理解写得太绝对了？

但我没有打算因此把它们全部删除。

因为删除很容易。

真正有价值的是保留：

我曾经为什么会这么认为。

一个人真正的成长，不应该只留下最后的正确答案。

也应该留下：

```text
当时的判断
    ↓
实践
    ↓
翻车
    ↓
重新理解
    ↓
修正
    ↓
新的体系
```

所以这里的模板仍然有价值。

但它们今天对我而言，已经不只是模板。

它们也是：

我早期关于 Agent 编排的思想样本。

九、现在回头看，三份文件真正留下来的东西

如果一定要把它们压缩成三个问题：

SOUL

我希望这个 Agent 成为什么样的参与者？

CONSTITUTION

哪些事情不能交给它自由决定？

AGENTS

在允许的范围内，我希望它怎样工作？

但还必须增加第四个问题：

Framework / Runtime

这些要求最终由什么机制来保证？

而这个第四个问题，是我后来才真正意识到的。

十、因此，这个仓库不是“标准答案”

这里的内容更适合被理解为：

一段 Agent 编排思想的实践记录。

它包含已经验证过的经验，也包含曾经过度相信的东西。

有些东西今天依然成立。

有些东西需要结合具体框架。

还有一些东西，我现在只保留为：

曾经观察到，但还没有继续研究的现象。

这也是为什么我没有删除早期版本。

因为：

错误的旧答案，有时候比漂亮的新答案更能证明一个体系是怎么长出来的。

最后

如果你只想从这个仓库带走一句话，我现在更愿意写成：

不要试图用 Prompt 替代系统。

让 Prompt 影响模型，让状态与工作流约束过程，让 Runtime 与权限约束动作，再用真实结果验证一切。

至于 SOUL / CONSTITUTION / AGENTS：

它们不是魔法。

它们是一种我在实践中逐渐形成的 Agent 编排语言。

而这套语言真正有价值的地方，

不是因为我一开始就知道答案。

恰恰是因为：

我曾经以为自己知道。

然后翻了车。

然后才开始真正理解它。